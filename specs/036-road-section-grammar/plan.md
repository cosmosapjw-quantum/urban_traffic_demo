# Implementation Plan: Road Section Grammar

- Expand `metroflow.map.lane_grammar` while preserving existing names.
- Add explicit design-standard widths and lateral-shift limit.
- Add immutable units, section ends, profiles, validation, and fingerprints.
- Keep all runtime traffic state and backend code untouched.
