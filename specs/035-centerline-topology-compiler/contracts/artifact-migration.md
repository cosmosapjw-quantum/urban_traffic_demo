# Hierarchy Artifact Migration

- `csur_module_signature` -> `road_hierarchy_module_signature`
- `csur_module_alignment_ok` -> `road_hierarchy_module_alignment_ok`

New artifacts emit only project-owned names. Historical diagnostic artifacts
remain readable as historical files but are not rewritten or treated as
compatibility evidence.
