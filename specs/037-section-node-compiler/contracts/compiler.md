# Section And Node Compiler Contract

Inputs are validated static nodes, links, and typed geometry. Outputs are
immutable static catalogs. Missing geometry, duplicate assignments, lane-count
drift, invalid capacities, or unresolved node endpoints fail closed.
