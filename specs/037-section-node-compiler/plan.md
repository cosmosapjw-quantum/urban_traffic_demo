# Implementation Plan: Section And Node Compiler

- Add `section_compiler.py` with immutable profile/assignment catalogs.
- Replace `node_compiler.py` placeholder with tangent-based static interfaces.
- Attach both catalogs during generated-topology finalization.
- Preserve RoadLink lanes/capacity exactly and record fingerprints in metadata.
