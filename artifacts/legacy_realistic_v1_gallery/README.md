# Legacy realistic_synthetic_v1 Gallery

These PNG artifacts were originally generated using the `realistic_synthetic_v1`
pipeline but were incorrectly committed as "scalable morphology" sample maps.

They have been moved here for provenance preservation. The current scalable
gallery in `artifacts/sample_city_maps/` uses the `scalable_synthetic_v2`
pipeline exclusively.
