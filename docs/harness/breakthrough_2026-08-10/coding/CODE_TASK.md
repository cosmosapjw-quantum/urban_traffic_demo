# Code Task Contract

DATE: 2026-08-10
TASK_TYPE: bounded implementation-plan design; no implementation authorization

## User-visible outcome

Produce an independently reviewable breakthrough execution plan for the current
scalable-map blockers using superseding H-002-R1 normalized-value ordering,
H-003 projection/registry in Task B, future source-owned capture, H-005, and the
H-006 control.

## Scientific purpose

Preserve exact city geometry/topology and deterministic authority identity while
closing canonical-stream memory, immutable snapshot, registry safety, and causal
instrumentation contract gaps.

## In scope

- Read-only localization of current Task B, Task 3B evidence, Task 4/5 seams, and
  G5/Task 6 documents.
- A separate-immutable-manifest strict-TDD replay plan for Task B.
- Exact normalized-value vectors, private schema/count rows, and honest
  OWNER_RED/REGRESSION_GREEN/SETUP_INVALID classification.
- Test, validation, review, rollback, and kill criteria.
- A docs-first future contract plan for causal G5 instrumentation.
- H-006 controls and H-001's evidence-only blocker disposition.

## Out of scope

- Production/test edits, implementation, official long runs, commit/push/PR.
- Task C integration/capture, Task 4/5 performance optimization, G5/Task 6 code.
- Task 3B optimization: H-001 is `REOPEN_EVIDENCE`, not a survivor.
- Traffic-flow, routing, demand, accessibility, land-use, policy, or transport
  algorithm work. No traffic-algorithm PR is open.

## Preserved behavior

- Exact integer/Fraction geometry, signed area/orientation, DCEL Euler invariant,
  tile partition/ownership, public bytes/fingerprints, replay, cold exhaustive
  validation, exact exception semantics, and baseline fallback.
- Only the two Task B files may eventually be replayed, and only after separate
  implementation authorization and a frozen completeness manifest.

## Acceptance for this run

1. Superseding research survivor handoff is hash-bound and no rejected mechanism is
   smuggled into the selected code plan.
2. Current defects and lifecycle gaps are localized to exact files/symbols.
3. The plan gives test-first slices, exact commands/expected REDs, minimal code
   seams, validation matrix, kill criteria, rollback points, and dependency gates.
4. An independent non-author reviewer finds no blocking plan defect.
5. Coding harness validates; project source/test hashes remain unchanged.

## Approval boundary

Implementation, reset/overwrite, long benchmark, commit, push, PR, Task C, and
traffic-algorithm work each require new explicit authority. This task ends at a
reviewed plan.
