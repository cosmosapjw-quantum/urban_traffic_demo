# Scalable Map Blocker Breakthrough Plan — R1

> **Execution boundary:** This is a reviewed-design candidate, not execution
> authority. Every checkbox is `NOT_AUTHORIZED` until the owner separately opens
> the named phase and its exact file scope.

**Goal:** Close the remaining Task B proof gaps without fabricating RED history,
then route stable capture, performance admission, Task 3B evidence, and G5 into
separate dependency-correct lanes.

**Selected architecture:** H-002-R1 virtual normalized-value ordering plus H-003
projection `P` and registry `R` in the two-file Task B replay. Stable capture `C`
belongs to a later Task 5 integration plan in `scalable_authority.py`. H-001 is
evidence-only. H-005 is docs-first after final prerequisite seals. H-006 remains
a falsifiable no-op/control lane.

**Current status:** plan only; no project source/test change, no long run, no
commit/push/PR, no Task C, and no traffic-algorithm work.

## 0. Bound authority and non-negotiable invariants

- Research closeout:
  `/tmp/physmath-research.5xTJ6x/state/CLOSEOUT.md`, SHA
  `42cc0e35ebd0ba5c72daab548f8709f84864e1aec5da2e65603620c85acb6be0`;
  survivor handoff SHA `726af7260e31fc40101b942e49ac2b642aae1f0f89609f1d8facc15135d0dd65`;
  research state SHA `14ab8b113a302541eb508efdb3b8cded967b1fcf68ed5e3a6b63a07425fe4841`;
  decision log SHA `78300d83a6c368da238df7bd644ed1328a7e5e093ec65a8cd63e22f622509f03`.
- H-002-R1 decision:
  `/tmp/physmath-research.5xTJ6x/state/H2_REOPEN_DECISION.md`, SHA
  `b660615d7044b25661c3edb9fbb95b8df7b041909663cd2508d221bb65238193`.
- Controlling Task4/5 brief SHA
  `41585f436832e37d734b80bd52c195ca7b922170cadf4d2b69894bb8609fcf6c`.
- Current project checkpoint: receipt source
  `5181eb4b03b87d91205a422a56183c14a59fc4299ce1cb83592244a4f07b4bdb`,
  focused tests
  `434442e01ae29919fda8712b17b70b5f66a84d0b66996fbf59bea64f1c930f06`,
  replay report
  `2f63435a05dcb52232dc89ce9bfc4cbfeee2419944bc687ac40f7c88f5e9d9d5`.
- Exact integer/Fraction geometry, physical units, DCEL `V-E+F=1+C`, tile
  ownership, public payload/fingerprint, semantic order, deterministic replay,
  exhaustive cold validation, and public exceptions are immutable observables.
- S2 before S1; no external-data learning; baseline fallback remains default.
- Task B production may not import Task 3/3B/4/5 modules. No trusted token,
  validation bypass, tolerance relaxation, approximate ordering, JSON-byte sort,
  hash sort, private type rank, public API expansion, or silent fallback.
- `apply_patch` is the only live text-edit mechanism. No reset/checkout/cleanup.
- Every future vector-generation, replay, focused, and dependency Python process
  in Tasks 1--8 uses `env PYTHONHASHSEED=0` and records the exact CPython
  implementation/version/executable/platform. Exact exception messages and
  frozenset iteration schedules have no authority outside that environment.

## 1. Mandatory pre-reset contract packet

**Files (future immutable design artifacts, separate from the mutable report):**

- `.superpowers/sdd/scalable_synthetic_v2_implementation_plan/task-b-replay-manifest-r2.md`
- `.superpowers/sdd/scalable_synthetic_v2_implementation_plan/task-b-replay-manifest-r2-independent-review.md`
- `.superpowers/sdd/scalable_synthetic_v2_implementation_plan/task-b-normalized-order-vectors-r1.json`
- `.superpowers/sdd/scalable_synthetic_v2_implementation_plan/task-b-normalized-order-vectors-r1-independent-review.md`

Creating these project artifacts requires explicit future file authority. Until
then, the plan-owned definitions are frozen in:

- `H2_ORDERING_CONTRACT.md`
- `CONTRACT_ROWS.md`

- [ ] **1.1 Independently review exact contract rows.** Require zero open
  P0/P1/P2 for all ten private annotations, the `np.ndarray` correction, signed
  seeds, identity-scoped mapping admission, all 30 record-count formulas, and
  malformed-incumbent disposition. Any rejected interpretation stops the replay;
  implementation may not choose a replacement.

- [ ] **1.2 Generate immutable normalized-value vectors from a test-owned literal
  reference.** Freeze original iteration order, materialized `N`, sorted result or
  exact exception type/message, whether collision scanning is reached, and final
  JSON bytes. Include every vector required by `H2_ORDERING_CONTRACT.md` in the
  separate immutable `task-b-normalized-order-vectors-r1.json`; production and
  the current serializer are forbidden as expected-value sources.

- [ ] **1.3 Build a separate immutable behavior manifest.** Every row is:

  ```text
  (slice_id, node_ids, classification, owner_behavior, exact_checkpoint,
   precondition, expected_discriminator, allowed_symbols, cumulative_gate,
   rollback_source_sha, rollback_test_sha)
  ```

  `classification` is exactly one of:

  - `OWNER_RED`: the behavior is absent and the exact node must fail from that
    absence before code exists;
  - `REGRESSION_GREEN`: an already fail-closed guard; it does not authorize code
    and is never misreported as a historical RED;
  - `SETUP_INVALID`: discarded/non-evidence, followed by byte restoration and a
    fresh run.

  A common `NotImplementedError` may own only the helper's first positive
  behavior, not every negative branch behind the same stub. Negative mapping or
  CSR guards that already fail safely are `REGRESSION_GREEN`. A hostile backing
  node may become `OWNER_RED` only after its exact positive admission exists and
  the missing guard demonstrably executes behavior.

- [ ] **1.4 Dry-run every proposed node in a scratch copy at its exact checkpoint.**
  Preserve command, prehashes, exit, failure line, and discriminator. No live
  replacement begins until every `OWNER_RED` is proven RED, every already-safe
  node is reclassified, and independent manifest review has zero P0/P1/P2.

- [ ] **1.5 Freeze manifest bytes once.** The immutable manifest is never the
  mutable replay report and is never appended after review. The mutable report
  records its SHA before each slice. Discovery of a new required owner behavior
  invalidates the manifest and stops the replay; it cannot be edited in place.

## 2. Exact clean-room recovery

The exact paired checkpoint is reconstructible from:

```text
/home/cosmosapjw/.codex/sessions/2026/08/09/
rollout-2026-08-09T19-35-18-019fe617-5d57-7551-bd34-d9ad8d861e9c.jsonl
line 4676, patch_apply_end, call_id
exec-ece78b79-2de9-4fca-949b-1713c10b261c
```

- source: `6,837` bytes, SHA
  `55734c7b04da7076b5d1c8b81ae50914e32b62ed1014f287e722ce78ddfd9c10`;
- tests: `1,348` bytes including final LF, SHA
  `5374e7aad34103f6cbfce307a0353c9829c22fcdb788f1c7752255aca5e5f42f`.

- [ ] **2.1 Verify session and extracted bytes without writing live files.**

  ```bash
  SESSION=/home/cosmosapjw/.codex/sessions/2026/08/09/rollout-2026-08-09T19-35-18-019fe617-5d57-7551-bd34-d9ad8d861e9c.jsonl
  sed -n '4676p' "$SESSION" | jq -jr \
    --arg p '/home/cosmosapjw/Dropbox/personal_projects/urban_traffic_demo/src/metroflow/city/scalable_validation_receipts.py' \
    '.payload.changes[$p].content' | sha256sum
  sed -n '4676p' "$SESSION" | jq -jr \
    --arg p '/home/cosmosapjw/Dropbox/personal_projects/urban_traffic_demo/tests/test_scalable_validation_receipts.py' \
    '.payload.changes[$p].content' | sha256sum
  ```

- [ ] **2.2 Make a `mktemp -d` recovery copy of current source/tests/report and
  hash it.** This is recovery evidence, not a reset.

- [ ] **2.3 Replace only the two authorized Task B files with `apply_patch`, then
  verify exact bytes.** If either checkpoint is unavailable or mismatched, stop.

- [ ] **2.4 Run only the API scaffold node.** Expect `1 passed`; all behavior
  helpers remain absent/stubbed.

  ```bash
  env PYTHONHASHSEED=0 .venv/bin/python -m pytest -q \
    tests/test_scalable_validation_receipts.py::test_exact_receipt_api_scaffold
  ```

## 3. Replay schemas and canonical scalar grammar

- [ ] **3.1 Annotation owner RED.** Add an API test asserting all ten records in
  `CONTRACT_ROWS.md`, especially
  `_Task5CsrProjection.arrays: tuple[tuple[str,np.ndarray],...]`. Observe the
  historical scaffold's `Any` mismatch, then change only annotations/imports.

- [ ] **3.2 Record constructors before any field consumer.** Stage exact type,
  subclass, malformed value, sorted/unique row, nested seal copy, and exact error
  precedence. One positive record branch owns the RED; already-safe negative
  cases are regression rows. Reconstruct every nested `_ContentSeal` before read.

- [ ] **3.3 Primitive normalization in independently owned slices.** Exact
  `None/bool/int/str`, finite float wrapper, Fraction wrapper, reviewed enum,
  tuple, dataclass, and frozenset each receive an unmasked owner or explicit
  regression classification. No multi-branch node may claim several hidden REDs.

- [ ] **3.4 Mapping admission by identity.** First the exact allowed dict leaf,
  then exact allowed proxy. Equal-but-unlisted, subclass, arbitrary Mapping, and
  unsupported leaf are regression guards if already safe. After positive proxy
  admission, separately stage hidden dict-subclass/arbitrary-backing behavior;
  only an observed behavior call can own its guard RED. Backing inspection is
  pinned to CPython 3.12 and may call no mapping method.

- [ ] **3.5 Base stream and seal.** Exact concatenated stream bytes, SHA-256,
  BLAKE2b-256, canonical byte count, and caller-supplied record count match the
  materialized reference for admitted values. Mapping collision remains after
  normalized pair sorting; equal-normalized frozenset members remain admitted.

## 4. H-002-R1 normalized-value ordering

- [ ] **4.1 Run immutable vectors against the current materialized and streaming
  paths.** The expected `2,10` numeric order must expose the current JSON-byte
  sort bug. Current-code agreement is not authority.

- [ ] **4.2 Stage two independent nonmaterialization RED parameters.** Exact
  frozenset and allowed exact dict use the literal fixture/boundary protocol in
  `H2_ORDERING_CONTRACT.md`: `common = "x" * 65_536`, 700 suffixes
  `f":{index:04d}"`, fixture construction then three GCs, then tracemalloc reset
  and wall start, consume only until a test-owned sort-completed spy fires, read
  peak/time, and stop without joining final output. Each must separately reach
  the retained-normalized-byte discriminator. `peak <= 33_554_432` bytes and
  elapsed `<=45_000_000_000 ns` are distinct gates.

- [ ] **4.3 Implement Algorithm A only after its test-local differential is
  separately authorized.** Use virtual `VEQ/VLT/PAIR_LT` over original references,
  pinned Python 3.12 `sorted`/Timsort via `cmp_to_key`, and complete stable
  preflight before emission. Do not implement a JSON-byte cursor comparator,
  private total rank, generic mergesort, or hash ordering.

- [ ] **4.4 Differential proof.** Exhaustive grammar through depth `3` and exactly
  `10_000` generated values from a local `random.Random(20260810)`, max depth `8`,
  must match the literal materialized-`N` reference in successful bytes and exact
  exception type/message. Freeze generator source, ordered corpus, and corpus
  SHA in the reviewed immutable vector packet before replay. Test tuple
  short-circuit, pair-value error before collision,
  enum/string and raw/synthetic collisions, cycles, named preflight-seam
  mutation, and no preflight output. Arbitrary post-preflight mutation is outside
  the claim and requires caller-owned stability through emission.

**H-002 kill:** any normalized-value/result/error/schedule drift, preflight
emission, retained normalized value/byte/tree/prefix, >32 MiB bounded test, or
>45 s long-prefix test.

## 5. Replay CSR arrays and projection algebra `P`

- [ ] **5.1 CSR positive owner then guard matrix.** First own the twelve valid
  canonical array rows. Each missing guard—unknown name, bool count, ndarray
  subclass, dtype metadata, wrong dtype, rank, shape, object dtype, non-C layout,
  validation-before-byte-read—may be `OWNER_RED` only if a scratch dry-run after
  the preceding slice reaches that exact missing guard. Otherwise mark it
  `REGRESSION_GREEN`; never roll all parameters into a common stub claim.

- [ ] **5.2 Canonical row implementation.** Validate before byte access, stream a
  fixed-size `memoryview(...).cast('B')` to both digests, and return only the
  seven-field row. No full copy belongs in row sealing.

- [ ] **5.3 Independent immutable copy.** Exact int32/float32/bool C arrays become
  bytes-backed, nonwriteable, C-contiguous copies with no dtype metadata or
  caller alias. Reject unsupported values before `tobytes`.

- [ ] **5.4 Deep snapshot records in dependency order.** Invocation, terrain,
  network, blocks, CSR, compiled, full seals, and verified snapshot each get a
  separate nested-alias/subclass/cycle test. Reconstruct module-owned nested
  records, tuple-copy rows, copy exact backing dicts before proxying, and copy
  arrays. Negative seeds remain valid exact ints.

- [ ] **5.5 Algebraic gates.** For the frozen grammar, `P(P(x)) == P(x)` and no
  caller-owned mutable object is reachable. The generic receipt module imports no
  Task authority module. Source-owned callers remain responsible for reviewed
  enum exactness.

## 6. Replay linearizable registry `R` — no stable capture here

- [ ] **6.1 Add only the module RLock and empty registry.** Then stage supported
  network absent miss / first insert / exact hit without descriptor mismatch,
  equality, collision, capacity, clear, or count.

- [ ] **6.2 Descriptor/stale matrix.** Cover all four exact authority grammars,
  every policy row, source/content name/schema omit/rename/add/reorder, stale
  deletion then separate exhaustive replacement, and direct unsupported
  preservation. Public fingerprint mutation is not a stale-field substitute.

- [ ] **6.3 Current-content comparator.** Reconstruct local validated receipt and
  current rows, compare content before sources, report the exact first named
  mismatch, and remain immune to mutation of the original after copy.

- [ ] **6.4 Whole-operation lock and malformed incumbent.** Lookup and register
  acquire the one RLock before descriptor/input copying and hold it through
  candidate reconstruction, comparison, transition, and eviction. Test spies may
  inspect `_is_owned()`; production does not. Malformed lookup deletes+MISS.
  Malformed registration deletes the incumbent, then re-raises the unchanged
  exact exception type/message produced by defensive reconstruction, without
  inserting the contender; a later exhaustive registration may insert.

- [ ] **6.5 Idempotence before collision/concurrency.** Same object and equal copy
  preserve incumbent identity. Only then stage unequal collision and deterministic
  barrier histories for equal/unequal stale recovery. All threads join before one
  aggregate assertion.

- [ ] **6.6 Capacity, clear, count, fresh process.** In disposable test-only maps
  installed/restored under the RLock, prove lexical smallest `512` independent of
  forward/reverse/seeded-shuffle/concurrent order and hits; then clear, exact
  count, and fresh child count zero.

**Scope correction:** Stable capture `C` is not in this two-file replay. The exact
functions `_capture_verified_task5_source_snapshot_from_receipt` and
`_capture_stable_static_validation_receipt` belong to a later authorized Task 5
integration plan in `src/metroflow/city/scalable_authority.py`. Task B may expose
only the already-reviewed records/seal/registry primitives; it may not invent a
capture seam.

## 7. Six-style 180-leaf equivalence

- [ ] **7.1 Use the six fixed public cases:** 100k/25 km2, seed 17, styles
  `grid_core`, `organic`, `polycentric_tod`, `ring_radial`,
  `river_constrained`, `superblock_mixed`.

- [ ] **7.2 Test-owned literal projection.** Build exactly the 30 leaves and
  counts from `CONTRACT_ROWS.md`; do not import private public-output payload
  helpers, registration/capture/projectors, or production expected seals. For
  each leaf compare materialized bytes, joined stream, SHA, BLAKE, byte count,
  and independent record count: 180 comparisons.

- [ ] **7.3 Preserve the natural adapter RED.** Evaluate real
  `task5.compiled_projection` first. Raw nested ndarray must initially fail.
  Implement only the exact `_Task5CsrProjection.arrays` field adapter through
  `_canonical_csr_array_row`; globally raw ndarray stays unsupported.

## 8. Exact Task B and dependency gates

Run only after all accepted slices and freeze raw output/hashes after each:

The ordered downstream collection must contain exactly `80` node IDs and its
newline-terminated ordered body must hash to
`70026af8a7763ea2a7ae0fbc546eb3406ca1262aeee573015ed38075705b4cc8`;
otherwise stop before execution.

```bash
env PYTHONHASHSEED=0 .venv/bin/python -m pytest -q tests/test_scalable_validation_receipts.py

env PYTHONHASHSEED=0 .venv/bin/python -m ruff check \
  src/metroflow/city/scalable_validation_receipts.py \
  tests/test_scalable_validation_receipts.py
env PYTHONHASHSEED=0 .venv/bin/python -m ruff format --check \
  src/metroflow/city/scalable_validation_receipts.py \
  tests/test_scalable_validation_receipts.py

env PYTHONHASHSEED=0 .venv/bin/python -m pytest -q \
  tests/test_scalable_topology_kernel.py tests/test_scalable_blocks_dcel.py

env PYTHONHASHSEED=0 .venv/bin/python -m pytest -q \
  tests/test_centerline_topology_compiler.py \
  tests/test_road_section_grammar.py \
  tests/test_section_node_compiler.py \
  tests/test_turn_compiler.py \
  tests/test_city_map_validation_closure.py \
  tests/test_metro_absorption_demand.py \
  tests/test_planar_city_blocks.py \
  tests/test_block_land_use.py \
  tests/test_realistic_city_map.py

env PYTHONHASHSEED=0 .venv/bin/python -m pytest --collect-only -q \
  tests/test_scalable_topology_adapter.py tests/test_scalable_authority.py
env PYTHONHASHSEED=0 .venv/bin/python -m pytest -q tests/test_scalable_topology_adapter.py
env PYTHONHASHSEED=0 .venv/bin/python -m pytest -q \
  tests/test_scalable_topology_adapter.py tests/test_scalable_authority.py

env PYTHONHASHSEED=0 .venv/bin/python tools/build_task45_preoptimization_oracle.py --verify \
  .superpowers/sdd/scalable_synthetic_v2_implementation_plan/task-4-5-preoptimization-byte-oracle.json
```

Any unrelated ordered-suite isolation failure is preserved and blocks the gate.
It is not fixed in the two-file lane and not regraded as Task B drift.

## 9. Deferred Task 5 integration `C` and separate performance admission

This phase is **not** opened by Task B completion. It requires a new exact
Task4/5 integration plan, current prerequisite hashes, production seams in
`scalable_authority.py`, and independent review. Only then may it run:

### 9.1 Official isolated RSS gate

The output report is an explicitly mutable append target. Record its prehash,
grant that one command append authority, and record its posthash.

```bash
env PYTHONHASHSEED=0 .venv/bin/python tools/run_task45_validation_performance.py \
  --mode isolated-rss \
  --target-population 1000000 \
  --urbanized-area-km2 250 \
  --style-id grid_core \
  --seed 17 \
  --replicates 2 \
  --output .superpowers/sdd/scalable_synthetic_v2_implementation_plan/task-4-5-validation-performance-report.md
```

Pass requires exit `0`, both deltas `<=32768 KiB`, and the exact repeatability
rule. This is not a wall-time result.

### 9.2 Separate fresh-process Task4+Task5 combined wall gate

Run exactly two fresh processes in the frozen environment. Task3/3B setup occurs
before the timer; Task4 compilation plus Task5 static build is the timed interval.
Each must print fingerprints and `combined_task45_ns <= 45_000_000_000`; any
nonzero exit or slower replicate fails. The future integration review must bind
the literal script below before execution.

```bash
set -euo pipefail
task45_wall_evidence_dir=$(mktemp -d)
for replicate in 1 2; do
  env TASK45_REPLICATE="$replicate" PYTHONHASHSEED=0 OMP_NUM_THREADS=1 \
    OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 \
    .venv/bin/python - <<'PY' \
    | tee "$task45_wall_evidence_dir/replicate-$replicate.txt"
import os
import platform
import sys
from time import perf_counter_ns

import numpy as np
from metroflow.city.scale import CityScaleSpec
from metroflow.city.scalable_topology import build_scalable_street_network
from metroflow.city.scalable_blocks import build_scalable_block_authority
from metroflow.city.scalable_topology_adapter import compile_scalable_topology
from metroflow.city.scalable_authority import build_scalable_static_authority

print(f"replicate={os.environ['TASK45_REPLICATE']}")
print(f"python={sys.version}")
print(f"executable={sys.executable}")
print(f"numpy={np.__version__}")
print(f"platform={platform.platform()}")
print(f"cwd={os.getcwd()}")
spec = CityScaleSpec(1_000_000, 250.0)
network = build_scalable_street_network(spec, "grid_core", 17)
blocks = build_scalable_block_authority(network)
started = perf_counter_ns()
compiled = compile_scalable_topology(network, block_authority=blocks)
static = build_scalable_static_authority(spec, "grid_core", 17, network, blocks, compiled)
elapsed = perf_counter_ns() - started
print(f"combined_task45_ns={elapsed}")
print(f"compiled_fingerprint={compiled.fingerprint}")
print(f"static_fingerprint={static.fingerprint}")
if elapsed > 45_000_000_000:
    raise SystemExit(4)
PY
done
sed -n '/_fingerprint=/p' "$task45_wall_evidence_dir/replicate-1.txt" \
  > "$task45_wall_evidence_dir/fingerprints-1.txt"
sed -n '/_fingerprint=/p' "$task45_wall_evidence_dir/replicate-2.txt" \
  > "$task45_wall_evidence_dir/fingerprints-2.txt"
cmp "$task45_wall_evidence_dir/fingerprints-1.txt" \
  "$task45_wall_evidence_dir/fingerprints-2.txt"
printf 'task45_wall_evidence_dir=%s\n' "$task45_wall_evidence_dir"
```

RSS and wall are separate mandatory gates. Passing one cannot offset the other.

## 10. H-001 Task 3B evidence-only reopening

- [ ] Write and independently review an evidence-only brief freezing mutually
  exclusive timer partitions and perfect-removal capacity for digest emission,
  canonical cycle, witness, clipping, and remaining validation.
- [ ] Run paired randomized no-op controls and exclusive-capacity diagnostics;
  do not edit production.
- [ ] If the non-overlapping removable upper bound is `<=7.531264014 s`, close
  the current optimization search. If larger, return to claim audit and external
  decision before any prototype. No result opens traffic work.

## 11. H-005 G5 docs-first repair

After Task B final review and a separately completed Task4/5 integration review:

- [ ] Freeze the current producer DAG, chronological order, separate
  presentation order, exact source-byte/count grammar, and actual ownership.
- [ ] `global_corridors` either owns a consumed immutable corridor plan or loses
  corridor authority to the real producer.
- [ ] Failure state is one completed prefix, one `FAILED_DURING_PHASE`, and every
  later node `NOT_REACHED_AFTER_FAILURE` with descendant/sibling reason.
- [ ] Re-raise the same exception with no rerun; compare independently with the
  smaller atomic-wrapper alternative.
- [ ] No timer/wrapper code until docs review has zero P0/P1/P2.

## 12. Review and decision separation

The final reviewer binds checkpoint, immutable manifest/vector reviews, every
accepted/discarded RED, final hashes, exact contract rows, all commands, outputs,
and scope. It issues separate verdicts for:

1. Task B current-code correctness;
2. strict-TDD admission;
3. H-002 memory/runtime admission;
4. Task C readiness;
5. Task 3B performance;
6. G5 authority;
7. traffic-algorithm readiness.

No aggregate score or transitive PASS is allowed. This plan currently authorizes
none of them.

## Frozen uncertainty and kill summary

- H-002 mathematical construction is conditional and not implemented.
- Mapping identity/backing semantics and every count row require independent
  pre-reset acceptance.
- Task B alone cannot run the official capture/RSS or integrated wall gates.
- Task 3B lacks exclusive capacity; G5 lacks a reviewed current DAG.
- Any new required behavior after immutable manifest freeze invalidates it.
- Any output/error/geometry/replay drift, false RED, alias, behavior execution,
  non-linearizable registry history, memory/wall failure, or scope expansion
  stops the corresponding lane.

PLAN_STATUS: REVISED_DESIGN_PENDING_INDEPENDENT_REREVIEW
IMPLEMENTATION_AUTHORITY: NO
