# Breakthrough Research and Coding Plan Backup

This directory preserves the minimal reviewed artifact chain produced on
2026-08-10 by the sequential physics/mathematics research loop and coding
research loop for the scalable synthetic city work.

## Status

- Research loop: complete, design only.
- Coding plan review: PASS - PLAN ONLY with open P0/P1/P2/P3 = 0/0/0/0.
- Implementation authority: **not granted**.
- Task 3B performance: still blocked by the less-than-20-second gate.
- Task B: breakthrough replay plan only; immutable vectors and behavior manifest
  must be created and independently reviewed before any reset or implementation.
- Task 4 dependency: correctness evidence remains separate from performance.
- Task 5 and G5: not ready; future receipt integration and docs-first causal DAG
  work remain gated.
- Traffic-algorithm PR: **not started**.

## Contents

- research/state/: bound research state and decision log, final closeout,
  survivor handoff, external decision review, and the corrected H-002
  analysis/validation/decision.
- coding/: scope and scientific contract, baseline acceptance, final
  breakthrough plan, ordering/schema contracts, initial blocking review, final
  passing rereview, run state, and coding-loop closeout.
- SHA256SUMS: byte-level checksums of every preserved source artifact.

These files are evidence and planning artifacts only. They do not claim that
the proposed algorithms are implemented, performant, or admitted.

Some source Markdown artifacts intentionally retain hard-break trailing spaces
and one source terminal blank line. A repository-wide diff whitespace check
therefore reports those known source-byte diagnostics. They are preserved
unchanged because SHA256SUMS freezes the reviewed bytes; do not normalize them.

The initial independent review is retained as historical BLOCK evidence and
binds superseded plan bytes. The independent rereview is the authority for the
included final plan and contract bytes.

Likewise, the H2 reopen analysis records an earlier survivor-handoff hash. The
included survivor handoff is the later final handoff; the H2 validation and H2
decision are the authority for the corrected reopened ordering claim.

The next gate is explicit authority to create and independently review the
immutable normalized-order vector packet and behavior manifest. It is not an
implementation gate.

## Source archive receipts

- Research harness ZIP SHA-256:
  9adde688f8020e7feb2c1c0304b3204dbe70dd01e2d87e64a5c4eb357c019934
- Coding harness ZIP SHA-256:
  6e67e999a0c19f6ed9de7c339067cc11691d5cf5cb662a11756d8fc393c849b4
- Research harness validator: exit 0.
- Coding harness validator: exit 0.

## Excluded on purpose

The temporary harness implementations, prompts, validators, ZIP archives,
session logs, project production code, project tests, manifests, and runtime
reports are not copied here. They are either reproducible machinery, unrelated
workspace state, or require a later separately authorized implementation gate.
